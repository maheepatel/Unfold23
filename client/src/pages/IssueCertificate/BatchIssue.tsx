import React from "react";
import { Typography } from "@mui/material";

const BatchType: React.FC<{}> = () => (
  <>
    <Typography variant="h4" sx={{ mb: 2 }}>Batch Issue</Typography>
  </>
);

export default BatchType;